stages:
- stage: 'Staging'
  displayName: 'Deploy to the staging environment'
  dependsOn: Test
  jobs:
  - template: azurewebapp_staging_step.yml
    parameters:
      appName: '$(WebAppNameStaging)'
      environment: 'staging'
            
          